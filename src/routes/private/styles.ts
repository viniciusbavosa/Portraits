import styled from 'styled-components';

export const Credentials = styled.form`
  display: flex;
  flex-direction: column;
  align-items: center;
  margin: 14px;

  label {
    margin-bottom: 14px;
  }

  input {
    margin-bottom: 14px;
  }

  button {
    padding: 4px;
    border-radius: 6px;
  }

`;